<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:listDivider">@drawable/divider_normal</item>
        <item name="android:listDividerAlertDialog">@drawable/divider_border</item>
    </style>

    <style name="LogoTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorWhite</item>
        <item name="colorPrimaryDark">@color/colorWhite</item>
        <item name="colorAccent">@color/colorWhite</item>
    </style>
    <!-- 全屏主题 Logo页使用 -->
    <style name="FullScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>
    <!-- 左右能填满的Toolbar样式 -->
    <style name="FillToolbar" parent="Widget.AppCompat.ActionBar">
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
    </style>

    <!-- Dialog从底部弹出动画 -->
    <style name="animTranslateBottom" parent="Animation.AppCompat.Dialog">
        <item name="android:windowEnterAnimation">@anim/translate_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/translate_out_bottom</item>
    </style>
    <!-- Dialog从顶部弹出动画 -->
    <style name="animTranslateTop" parent="Animation.AppCompat.Dialog">
        <item name="android:windowEnterAnimation">@anim/translate_in_top</item>
        <item name="android:windowExitAnimation">@anim/translate_out_top</item>
    </style>

    <!-- 下载对话框样式  @android:style/Widget.ProgressBar.Horizontal-->
    <style name="ProgressBarDownload" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:maxHeight">20dp</item>
        <item name="android:minHeight">12dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">
            @android:drawable/progress_indeterminate_horizontal
        </item>
        <item name="android:progressDrawable">@drawable/shape_progressbar_blue</item>
    </style>
    <!-- H5页面进度条样式 -->
    <style name="ProgressBarH5" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:maxHeight">4dp</item>
        <item name="android:minHeight">4dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">
            @android:drawable/progress_indeterminate_horizontal
        </item>
        <item name="android:progressDrawable">@color/colorYellow</item>
    </style>






    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!--  圆角dialog -->
    <style name="dialog_radius" parent="Theme.AppCompat.Dialog">
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowFrame">@null</item><!--边框-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">true</item><!--半透明-->
        <item name="android:windowNoTitle">true</item><!--无标题-->
        <item name="android:background">@drawable/shape_radius_white</item><!--dialog背景样式-->
        <item name="android:windowBackground">@color/transparent</item><!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">false</item><!--模糊-->

        <item name="android:windowEnterAnimation">@anim/translate_in_top</item>
        <item name="android:windowExitAnimation">@anim/translate_out_top</item>

    </style>
</resources>
